jdk:
  - openjdk21
before_install:
  - sdk install java 21.0.2-open
  - sdk use java 21.0.2-open
install:
  - echo "Running a custom install command"
  - cd android && ./gradlew lyrazohoandroid:publishToMavenLocal
