package com.lyracore.zoho.core.models.enums

/** Enum representing different locations errors of events that can occur and be handled. */
enum class ErrorLocation {
    CORE_INITIALIZE,
    CHAT_OPEN,
    CHAT_GET_DEPARTMENTS,
    CHAT_END_CHAT,
    CHAT_SHOW_ZOHO_LAUNCHER,
}
